on:
  push:
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get the version
        run: echo "Hello World"

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get the version
        run: echo "Hello World"
